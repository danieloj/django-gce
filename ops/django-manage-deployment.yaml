apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: django-manage
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: django-manage
        tier: frontend
    spec:
      containers:
      - name: django-manage
        image: gcr.io/django-172117/djangodocker-manage:latest
        env:
          - name: DJANGO_DB_HOST
            value: postgres
          - name: DJANGO_DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: postgres
                key: password
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
